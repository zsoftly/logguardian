############################################################
#  Default Environment Configuration (.env)
# This file contains environment variables for both
# LocalStack (mock AWS) and Real AWS testing.
# 
# 👉 To switch context:
# - For LocalStack: keep the LocalStack section uncommented.
# - For Real AWS: comment out the LocalStack section and 
#   uncomment the Real AWS section below.
# 
# Note: NEVER commit real AWS credentials or sensitive data.
############################################################

############################################################
#  LOCALSTACK CONFIGURATION — for local integration tests
# Use this when testing locally with dockerized LocalStack.
# Command:
#   docker run --rm -it -p 4566:4566 localstack/localstack
#   go test -tags=integration ./...
############################################################
APP_ENV=integration
LOCALSTACK_ENDPOINT=http://localhost:4566
AWS_REGION=ca-central-1
BUCKET_NAME=test-bucket
OBJECT_KEY=test-object.txt
TEST_BODY=hello-localstack


############################################################
#  REAL AWS CONFIGURATION — for actual AWS integration
# Use this ONLY when validating against real AWS resources.
# Command:
#   unset LOCALSTACK_ENDPOINT
#   go test -tags=integration ./...
#
# ⚠️ IMPORTANT:
# - Make sure you have valid AWS credentials configured (via CLI or env).
# - Replace placeholder values with your actual AWS bucket name, etc.
# - Comment out the LocalStack section above before running.
############################################################
# APP_ENV=prod
# AWS_REGION=ca-central-1
# BUCKET_NAME=your-real-bucket-name
# OBJECT_KEY=integration-test/object.txt
# TEST_BODY=hello-from-aws
